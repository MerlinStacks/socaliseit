# =============================================================================
# SocialiseIT Production Environment Variables
# Copy this file to stack.env and fill in real values
# =============================================================================

# -----------------------------------------------------------------------------
# Application URLs
# -----------------------------------------------------------------------------
# The public URL where your app will be accessed (set in NPM)
APP_URL=https://socialiseit.yourdomain.com

# -----------------------------------------------------------------------------
# Security (CRITICAL - Generate secure values!)
# -----------------------------------------------------------------------------
# NextAuth secret - generate with: openssl rand -base64 32
AUTH_SECRET=CHANGE_ME_GENERATE_NEW_SECRET

# Encryption key for OAuth credentials (32 bytes, base64)
# Generate with: openssl rand -base64 32
# WARNING: Changing this will break existing encrypted credentials!
ENCRYPTION_KEY=CHANGE_ME_GENERATE_NEW_KEY

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=socialiseit

# -----------------------------------------------------------------------------
# MinIO / S3 Storage
# -----------------------------------------------------------------------------
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# Internal endpoint (Docker network)
S3_ENDPOINT=http://socialiseit-minio:9000

# Public URL for media access (optional - if proxied via NPM)
# S3_PUBLIC_URL=https://cdn.socialiseit.yourdomain.com

# Bucket name
S3_BUCKET=socialiseit

# -----------------------------------------------------------------------------
# OAuth Providers (from developer portals)
# -----------------------------------------------------------------------------
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# -----------------------------------------------------------------------------
# NOTE: Social Platform APIs (Instagram, Facebook, TikTok, YouTube) and AI keys
# are configured via Settings UI → Platform Credentials / AI Settings
# They are stored encrypted in the database, not in environment variables.
# -----------------------------------------------------------------------------
