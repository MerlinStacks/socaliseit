/**
 * Platform Publishing Module - Shared Types
 * Why: Re-export types used by all platform publishers.
 */

// Re-export from main types file
export type { PlatformAccount, PublishPayload, PublishResponse } from '../types';
